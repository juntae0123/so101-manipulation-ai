# DEVLOG 2026-09-10 — 사이클 1

> **이 파일의 용도**: 사용자와 어시스턴트가 **둘 다 보는 단일 상태판**이다.
> 「상태 보드」는 매 턴 갱신한다. 「시간순 기록」은 append 만 한다 (덮어쓰지 않는다).
> 무엇을 했는지 헷갈리면 여기를 먼저 본다. 여기 없으면 안 한 것이다.

---

## 상태 보드 (최종 갱신 2026-09-11, **실물 카메라 구성 확정 — 배포 구성 불일치 발견**)

### 🟢 끝난 것

| 무엇 | 증거 |
|---|---|
| 서버 미추적 14파일(0909~0910 MEASURE·PREREG·도구) 커밋 | 사용자 실행 |
| 서버 ↔ origin 동기화 (PAT 인증 복구) | 사용자 실행 |
| `probe_gripper_schedule.py` 인자 추가 (`--no-expected`/`--claim-name`/`--oracle-only`/`--oracle-tolerance`) | `099def9` |
| E1·E2·E3 사전등록 + 러너 | `6ff50e0` |
| `.gitattributes` 자기 자신 규칙 (CR 36줄 노이즈 종결) | `71dd1d4` |
| **A: 오늘 트리 기준값 확정 — 2회 실행 완전 동일 → 롤아웃은 결정적** | `65370da`, EXP_LOG |
| 그리퍼 이진 헤드 사전등록 | `cc129b0` |
| **E1 축소판 완료 — v5 팔의 천장을 37% 로 실측** | `MEASURE_gripper_schedule_e1_v5_0910.md` |
| 사전등록 addendum — 그리퍼 실험에 DAgger 데이터 조건 추가 | `..._addendum.md` |
| LIMITS L71(verdict 임계값 결함)·L72(oracle 고장) 등재 | `LIMITS.md` |
| 포트폴리오 19건 동기화 — 해시 전부 일치 | 폴더 연결 후 |
| 큐 러너 + `--action-space` + `AI_CLAIM_NAME` | `4bb978e` |
| `joint_delta_gripper_binary` 구현 + fixture 15/15 통과 | `a95061c` |
| 도구가 호출자 `PYTHONPATH` 에 의존하지 않게 수정 | `64a4a27` |
| **UMI raw 번들 1편 수령 검사 — 시연 아님, 학습 불가 판정** | `MEASURE_umi_bundle_intake_0910.md` |
| 프로젝트 문서 `claude/규칙_정정_Git_MR.md` 에 오늘 사고 4건 추가 | 프로젝트 |
| S1 실행 포락선 — **ROS2 경로 확정** (84~100% 통과, 위반은 URDF 5자리 절삭 산물) | `MEASURE_exec_envelope_0911.md` |
| 관측 절제 — **정책이 이미지를 쓴다** 확인. 손목이 front 의 4.5배 | `MEASURE_obs_ablation_0911.md` |
| **실물 카메라 구성 확정** 🔵 — UMI 그리퍼 1대 + 로봇팔 1대, 같은 각도. **3인칭 없음** | 사용자 구두 |
| L62 해소 · L76 등재 (현행 체크포인트는 실물에 없는 입력을 요구한다) | `LIMITS.md` |
| 카메라 부분집합 학습 경로 (`--cameras`) — dataset·train_bc·repeat·queue 4단 통과 | 미커밋 |

### ✅ 큐 완료 (101분)

```
B-dagger  6/69/25  평균 33.3%  범위 폭 63%p   rc=0 게이트 통과
B-v5     21/23/21  평균 21.7%  구간하한 17.4% rc=1 게이트 실패
```
판정: 처방 작동 확인(미폐쇄 42→4~27, dagger 닫는순간 4.9mm=scripted 수준).
**"배포 가능"은 유보** — dagger 범위 폭 63%p.

### 🔴 지금 가장 중요한 것 — 배포 구성 불일치 (L76)

시뮬·계약은 카메라 2대를 **동시에** 먹인다. 그중 `cam_front` 는 `kind: fixed`,
`parent: world` 인 **3인칭 고정 카메라**다 🟢. **실물에는 그 카메라가 없다** 🔵.

⇒ **6/69/25 는 배포 구성 기준 상한이다. 현행 체크포인트는 그대로 실물에 못 올린다.**

비용 근사치는 이미 있다 🟢 (`donor_front`, 완전 짝지어진 비교):
seed1 69→55, seed2 25→12 — **약 −14%p**. 단 재학습 없는 수치라 **상한**이다 🟡.

**다음 실행: 손목 전용 재학습** `queue/pending/wrist_only_dagger.yaml`
· 사전등록 `PREREG_wrist_only_0911.md` · 데이터 재수집 없음 (기존 에피소드에서 손목만 읽는다)

### ⏳ 도는 중(이전)

- **큐 소진** — `gripper_binary_v5` + `gripper_binary_dagger`, `--parallel 2`.
  진행은 `out/queue_20260910_162850/*.log` (콘솔에는 완료 시에만 찍힌다)
  첫 관문 통과 🟢: 닫힘 라벨 28.6% · pos_weight 2.493 · 그리퍼 채널 표준화 std 1.0 ·
  자명한 예측기(항상 열어둠) 3.62 대비 epoch3 val 0.699

### ⬜ 안 한 것 / 대기

- **큐 러너 `tools/run_queue.py`** — 착수. `runtime_limits.claim` 환경변수 override 포함
- **그리퍼 이진 헤드 구현 (`policy/bc.py`)** — 착수 조건 통과(fixed 최고 37% >= 30%). 큐의 첫 항목
- `TS_v5_fixture_drift_0910.md` — 14/12/2 fixture 사고의 TS. 미작성
- `KPT_0910.md` — 사이클 종료 시
- FastAPI 이슈 `S15P21A103-131` 범위 확정 — v5 결과 후로 사용자가 미뤄둠

### 🚫 폐기·철회

- **E2 (oracle tolerance 스윕)** — 폐기. oracle 계측기 고장은 남아 있으나 진단 정교화라 3일 안에 가치 없음
- **E3 (DAgger seed 3·4)** — 폐기. 같은 이유
- ~~E1 전체(1800편)~~ → 축소판(600편)으로 대체

### 🛑 막힌 것

- ~~포트폴리오 폴더 미연결~~ → 해소 (2026-09-10 연결, 19건 동기화)
- **oracle 계측기 고장** — v5 에서도 재현(3/100 < learned 11/100). E2 폐기로 미해결 → **L72 등재**
- `probe_gripper_schedule.py` verdict 가 체크포인트 1~2개 실행에서 무의미 → **L71 등재**
- **UMI 실데이터에 `gripper.csv`(gap_m) 가 없다** — 개폐 라벨을 만들 수 없다. MW·트랙 A 요청 대기
- **`summary.txt` 의 `usable_segments` 정의가 SPEC 과 충돌** (안정화 폐기 구간 미반영). 트랙 A 확인 필요
- **큐가 도는 동안 소스가 바뀌면 같은 실험 안에서 코드가 갈린다** — `repeat_runs` 가
  seed 마다 `train_bc.py` 를 새 프로세스로 띄우기 때문. `run_queue` 는 시작 시 1회만
  dirty 검사한다. HEAD 고정 가드 필요 (큐 완료 후 착수)

---

## 인용 가능한 확정 수치 🟢

**트리 `65370da` · seeds 3000~3099 · jitter ±50mm · render · policy-device cpu · n=100 · 2회 실행 동일**

```
scripted    84/100  닫는 순간 중앙  4.7mm  미폐쇄  1/100  실패 최근접 중앙  0.3mm
bc v5 seed0 11/100  닫는 순간 중앙 28.0mm  미폐쇄 42/100  실패 최근접 중앙 14.3mm
hold 0/100 · zero 0/100
```

⚠️ **`14/12/2` 는 트리 `1195c41` 의 값이다. 인용 금지.** 그 이후 `eval/`·`sim/`·`policy/`·`so101.yaml` 을 15커밋 건드렸다.

DAgger 계열 (트리 `5fbbcdf`): learned 1/0/7 · fixed 최고 86/49/44

**E1 축소판 (트리 65370da 이후, seeds 3000~3099, n=100) 🟢**

```
sim_pick_v5_seed0   learned 11 · fixed60 18 · fixed67 35 · fixed75 36 · fixed85 37 · oracle 3
```

→ **v5 팔 + 완벽한 그리퍼 = 37% 가 천장.** DAgger 팔은 같은 개입으로 86%.
차이 49%p 가 팔 정밀도의 몫이다. `verdict` 필드는 무효 (L71).

---

## 시간순 기록 (append only)

**인수인계** — 다른 대화가 오류로 끊겨 이 세션이 이어받았다. E1/E2/E3 초안을 저장·커밋하는 지점이었다.

**E1/E2/E3 초안 결함 6건 발견·수정** — 그중 2건은 그대로 돌렸으면 아무것도 안 했다:
heredoc 종료자가 본문 끝에 붙어 스크립트 전체가 마크다운으로 먹힘(E2·E3),
`repeat_runs` 가 게이트 실패 시 exit 1 이라 `set -e` 로 죽음(E3).

**서버 상태 파악** — 커밋 4건 미푸시 + **미추적 14파일**. 그중 `MEASURE_gripper_schedule_0910.md`,
`MEASURE_freeze_dagger_parallel_0910.md` 가 git 밖이었다. 계측 기록 소실 위험이 E1 보다 우선이라 판단, 먼저 커밋.

**A (기준값 확정)** — `eval_rollout` v5 seed0. 예측 "11 이 나온다" 적중 🟢.
두 번째 실행이 완전히 동일 → **롤아웃은 결정적**. 비결정성 갈래 배제.

**새 발견 — 미폐쇄 42/100.** scripted 는 1건. `closing_moment` 정의상 "에피소드 전체에서
그리퍼 명령 진폭이 문턱 미만" 이다. 회귀가 조건부 평균에 눌러앉은 모양.

**E1 폐기 → 축소 재개** — A 결과로 v5 실패 최근접이 14.3mm 임이 드러났다.
"팔은 배웠다"가 v5 에 성립하지 않는다. "v5 팔 + 완벽한 그리퍼 = 몇 %인가"가
그리퍼 작업의 착수 조건이 되므로 축소판(600편)으로 되살렸다.

---

**E1 결과 — 예측 적중 🟢.** "v5 fixed 최고 30~50%" 예측에 실측 37%.
0909 의 미해결 질문("fixed 성공이 DAgger 덕분인가 그리퍼 덕분인가")에 답이 나왔다:
**둘 다이고 팔 쪽 몫이 더 크다.**

**UMI raw 번들 1편 수령** — 사용자가 `rec_20260910_172751.zip` 을 올렸다.
계약 대조 결과 `MEASURE_umi_bundle_intake_0910.md`. 카메라 설정(OIS/EIS off, AE/AWB
lock)·시계(IMU skew 7.34ms)·pose↔frame 1:1(141/141)·30.22Hz 는 전부 SPEC 만족 🟢.
그러나 **프레임 육안 확인 결과 시연이 아니다** — 손에 든 폰으로 사무실을 훑은 영상이고
그리퍼·마커·물체가 없다. 게다가 `gripper.csv` 가 없어 개폐 라벨을 만들 수 없고,
길이 4.67초는 SPEC 의 "초기 3~5초 폐기" 와 충돌한다. **학습 투입 불가.**

**시뮬 자동화 파이프라인 논의** — 사용자 제안. 결론: 실행은 자동화, **판단은 자동화하지
않는다.** n=100 에서 ±9%p 흔들리는데 기계가 조합을 훑고 최고를 고르면 다중비교 과적합이다.
만들 것은 **사전등록 큐 러너** — 사전등록 파일이 없으면 실행을 거부한다 (기계가 원칙을 강제).

---

**S1 실행 포락선 (2026-09-11)** — 로봇 없이 "정책 명령열이 실물 API 를 통과하는가"를 쟀다.
ROS2 경로 84~100% 통과, 위반은 전부 `max_ratio` 정확히 1.0000x 로 **URDF 5자리 절삭**
산물이다 (차이 2.73e-6 rad). 클램프 비용 ±0.0%p 🟢. direct-motor 경로는 x20(1.5Hz)에서야
통과 — **30Hz 정책에 부적합** (L75). **실행 계층은 병목이 아니다.**

**관측 절제 (2026-09-11)** — 폐루프에서 이미지를 기증자 에피소드 것으로 바꿨다.
`donor_both` 에서 seed1 69→10, seed0·seed2 는 0 — **정책은 이미지를 쓴다.**
"관측 미사용" 가설 기각 ⇒ **팔 정밀도가 진짜 병목**이고 행동표현·손실·데이터 작업에
근거가 생겼다. 판정 함수의 바닥 효과 결함을 같은 커밋에서 고쳤다.

---

**실물 카메라 구성 확정 (2026-09-11)** 🔵 — 사용자 구두:
"우리 카메라 2대는 UMI 그리퍼와 로봇팔에 하나씩 같은 각도로 촬영하는 게 끝이다."

두 카메라는 **같은 시점을 두 번 구현한 것**이다 — 시연 수집용 1대, 실행용 1대,
동시에 켜지지 않는다. **추론 시점 이미지는 1장.** 실물에 3인칭 카메라는 없다.

이것이 바로 앞의 절제 결과를 다시 읽게 만든다. "손목이 front 의 4.5배"는 배치 우선순위가
아니라 **손목이 유일한 카메라**라는 뜻이고, "front 도 14%p 기여한다"는 **실물에서
회수 불가능한 이득**이라는 뜻이다. L62 를 '미정의'에서 '없음'으로 해소하고 L76 을 열었다.

조치: `EpisodeDataset` 의 `camera_names` 를 부분집합 선택으로 바꿔 **재수집 없이**
손목 전용 학습이 되게 했다. `--cameras` 를 train_bc → repeat_runs → run_queue 4단으로
통과시켰다. 큐 항목 `wrist_only_dagger` 등록.

**큐 상태 정정** — `queue/pending/` 에 이미 끝난 `gripper_binary_dagger`·`gripper_binary_v5`
가 추적된 채 남아 있었다. 서버에서만 `done/` 으로 옮겨졌고 그 이동이 커밋되지 않았다.
그대로 큐를 돌리면 **101분짜리 실험을 다시 돌린다.** `done/` 으로 옮겨 커밋한다.

---

---

## 2026-09-12 — 손목 전용과 VLM M0

**손목 전용 재학습 (L76 해소)** 🟢 — 실물에 없는 3인칭 카메라를 빼고 같은 데이터·시드로 다시 학습했다.

```
카메라 2대   6 / 69 / 25   평균 33.3%   범위 폭 63.0%p   100/300 [28.2, 38.8]
손목 전용   55 / 45 / 78   평균 59.3%   범위 폭 33.0%p   178/300 [53.7, 64.7]
```

**+26.0%p 이고 합산 구간이 겹치지 않는다.** 배포 구성에 맞추는 것이 성능 희생이 아니었다.
학습 실행 간 편차도 절반으로 줄었다 — "범위 폭 63%p" 가 지금까지 배포를 막던 사유였는데
그 절반이 3인칭 카메라에서 오고 있었다는 뜻이다 🟡(미구분).

계약을 `count: 1` 로 바꾼다 → D-AI-39.

**VLM M0 (D-AI-17 게이트)** 🟢 — 파인튜닝 전 베이스 모델 스킬 선택.

```
Qwen2.5-VL-3B      0.75    render − blank = +0.00
SmolVLM2-500M      0.20    100건 전부 pick_place 로 찍는 상수 예측기
```

이미지 기여가 **정확히 0** 이다. 예측 적중 🟡→🟢 — 스킬 5종은 놓기 단계로만 구분되고
그 차이는 사진에 안 나온다. **M0 가 잰 것은 지시문 분류 정확도다.**
스킬 선택용 LoRA 를 착수하지 않는다 → D-AI-40. 다음은 grounding(bbox).

경량 후보가 사라진 것이 더 큰 문제다 — SmolVLM-500M 이 붕괴했으므로
Jetson 8GB(이슈 42) 안에 정책과 함께 들어갈 VLM 이 현재 없다.

**실물 데이터** — UMI 번들 71편(6,087프레임) 수령·검사. 실제 시연이다.
`gripper.csv` 가 없어 마젠타 마커 검출로 직접 생성했다 — 협업 트랙이 낸 1편을 fixture 로
두고 재현 검정(중앙 0.346mm · 최대 1.129mm · 상관 0.999556 · 폐쇄 전이 프레임 일치) 후 71편 생성.
닫힘 비율 0.554 로 라벨 균형이 시뮬보다 좋다. 폐쇄 이벤트가 68/71편에 있다.

**남은 블로커** — 시뮬과 실물이 같은 그리퍼 채널에 다른 규약을 쓴다.
이진 임계값이 시뮬 설정에서 파생돼 실물 데이터의 닫힘 라벨이 **0/76** 이 된다.
임계값을 데이터에서 뽑도록 수정해야 실물 학습이 출발한다.

---

## Problem — 어시스턴트 자신의 오류 (덮지 않는다)

| # | 오류 | 어떻게 드러났나 | 조치 |
|---|---|---|---|
| 1 | `push_both.sh` 를 잊고 `git push origin ai` 만 안내 | 사용자 지적 | 프로젝트 문서에 규칙 추가 + 메모리 |
| 2 | `MUJOCO_GL=egl` 누락 | `mujoco.FatalError` | 프로젝트 문서에 실행 전제 절 추가 |
| 3 | `PYTHONPATH` 누락 | `ModuleNotFoundError: runtime_limits` | 위와 같음 |
| 4 | **관측값 11 을 낡은 값 14 로 덮음** | E1 fixture 실패 | `git_rev` 대조 규칙 추가. 초안(11/10/2)이 옳았다 |
| 5 | stash 목록에 `FINDINGS.md` 누락 | `cannot pull with rebase` | merge 로 전환 |
| 6 | "손실 크기 때문에 그리퍼가 묻힌다" | `target_scale` 이 채널별 표준화임을 확인 | 철회. 크기가 아니라 손실 **형태** 문제 |
| 7 | "팔은 배웠다"(DAgger 근거)를 v5 에 전용 | A 의 최근접 14.3mm | 철회 |
| 8 | E1 폐기 결정 | A 결과 | 되돌려 축소판으로 재개 |
| 9 | **DEVLOG·TS 를 하루 종일 안 씀** | 사용자 지적 | 이 파일. 매 턴 상태 보드 갱신 |
| 16 | **E2(oracle 계측기 수리)를 E3 와 묶어 "진단 정교화"로 접었다** | 사용자가 뒤집음 — 계측기 수리는 정교화가 아니다 | 되살린다. 첫 답은 `result.json` 에 이미 있다 (공짜) |
| 13 | 예측 **판정1(미폐쇄<=5) 통과** — 둘 다 실패했다 (dagger 67/20/40, v5 27/4/8) | 이진화가 중간값 붕괴는 없앴지만 "닫을 때를 못 봄"은 남는다 |
| 14 | 예측 **판정2 둘 다 실패** — dagger 는 통과했다 (3.7~5.9mm) | 데이터가 좋으면 폐쇄 정밀도가 scripted 수준까지 간다 |
| 15 | 사전등록의 "141스텝 중 약 110이 열림" 이 **v5 에 대해 틀렸다** (실측 53.2% 닫힘) | 메커니즘 서술을 dagger 기준으로 일반화했다 |
| 10 | 사전등록에 손실을 **MSE** 로 적음 | `configs/train/bc.yaml` 이 `loss: l1` | 정정. 메커니즘이 조건부 평균 → **중앙값** 으로 달라진다 |
| 11 | **`PYTHONPATH` 누락 3회** (#3 과 같은 계열) | 큐가 `No module named 'policy'` 로 안 돎 | 규칙 추가가 아니라 **구조로 막았다** — `run_queue.py`·`probe_gripper_schedule.py` 에 `sys.path.insert` 추가. 저장소의 다른 `tools/` 는 전부 하고 있었다 |
| 12 | `push_both.sh` 를 안내에서 또 빠뜨림 | 사용자 지적 | 명령 블록에 항상 포함 |
| 17 | **`verdict()` 에 바닥 효과를 반영 못 함** — 사전등록 예측 4번에 "seed0 은 절대 %p 로 읽는다"고 적어놓고 함수에는 안 넣었다 | seed0 이 100% 를 잃었는데 `images_barely_used` 출력 | 같은 커밋에서 수정. 절대 하락 + 상대 상실률 + 최근접 배율 3중 판정 |
| 18 | **`cam_front` 를 실물 카메라로 가정한 채 절제 결과를 해석** — "2대 구성 논의의 근거"라고 썼다 | 사용자가 실물 구성을 알려줌 (3인칭 없음) | 정정. 근거였던 14%p 가 **회수 불가능한 이득**으로 뒤집힌다. L62 해소·L76 등재. **실물 구성을 확인하지 않고 시뮬 수치를 배포 근거로 썼다** |
| 19 | **끝난 큐 항목이 `pending/` 에 추적된 채 남은 것을 못 봄** | 새 항목 넣다가 발견 | `done/` 이동을 커밋한다. 안 봤으면 101분 재실행 |
| 20 | **절제 수치를 재학습의 상한으로 인용** — `donor_front` −14%p 를 근거로 손목 전용이 8~18%p **하락**한다고 예측 | 실측 **+26.0%p 상승** | 입력을 망가뜨리는 절제는 그 입력을 제거한 모델을 예측하지 않는다. L77 등재. 예측 3개가 방향까지 전부 틀렸다 |
| 21 | **gripper.csv 재구현을 유효성 검사 없이 돌림** | gap 이 −0.6~376.3mm (물리 상한 90mm) | 크기비·형상·물리범위 검사 3종 추가. 계측기가 조용히 불가능한 값을 뱉었다 |
| 22 | **백그라운드 job 이 device_bash 호출 종료와 함께 죽는 것을 모르고 nohup 으로 돌림** | 7/71 에서 멈춘 것을 뒤늦게 발견 | 호출당 timeout 으로 쪼개고 `--skip-existing` 으로 재개 |
