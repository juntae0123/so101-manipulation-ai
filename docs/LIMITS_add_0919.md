# LIMITS 추가 — 2026-09-19

작성 김준태(트랙 B). 번호는 붙이지 않는다 (LIMITS 본문 병합 시 부여).

## VLM
- **Qwen 0.83 [0.74, 0.89] n=100 은 제품 정확도가 아니다.** 지시문 100개를
  어시스턴트가 작성했다. 실사용자는 더 모호하게 말한다
- **②(target grounding) 미측정.** 대상물이 하나인 동안만 안 걸린다 (D-AI-70)
- **③(거부) 미측정.** 현재 강제선택 구조에서는 출력 자체가 없어 구조적으로 0%
- "이미지가 기여하지 않는다" 는 관측 2회(0912 렌더 · 0914 M1)에 근거한다.
  **현행 diffusion 정책에는 적용해본 적이 없다** — 그건 별도 측정
  (`PREREG_empty_scene_refusal_0919`)

## hand-eye
- **원점 t 는 provisional 이다** (0919 격하). 마커 중점 = 물리 패드 중심이라는
  직접 증거가 없고, 0918 raw 기준 CAD TCP 와 약 32.9mm 차이가 보고됐다.
  회전축만 확정 🟢

## 어댑터
- jaw 게이트·손목 후보 선택·측지각 검사 전부 **시뮬 모델에서만 검증**했다.
  실물 모델에서 `--probe-tcp` 를 돌린 적이 없다
- wrist_roll 상한 +60도는 **마진값**이다. 브래킷 실측은 +64.86도

## 계측 미실행
- `sweep_camera_mount.py` — 실행 중. **결과 전까지 허용 공차 인용 금지**
- `probe_empty_scene_refusal.py` — 미실행 (프레임 미촬영)
- `probe_vlm_refusal.py` — 미실행. 로컬 자체검증 8/8 중 4행은 torch 없어 건너뜀
- `solve_grasp_anchor.py` — 초판 결과(0/720)는 도구 오류. 수정본 미실행
