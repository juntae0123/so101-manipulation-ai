# PREREG — 손목 전용 시드 3·4 추가 (2026-09-12)

작성자: 김준태(트랙B) · 이슈 S15P21A103-34 · `PREREG_wrist_only_0911.md` 의 후속
큐 항목: `queue/pending/wrist_only_dagger_s34.yaml`

## 왜 — 게이트를 옮기는 게 아니라 n 을 올린다

손목 전용 3시드 실측 🟢: 55 / 45 / 78 · 평균 59.3% · **범위 폭 33.0%p**.

이 프로젝트 규칙이 명시한다 — **"게이트를 옮기지 말고 n 을 올려라. 게이트 변경은 사후
합리화, n 증가는 정밀도 개선이다."** 33%p 폭은 "이 설정이 내놓는 정책"의 분산이고,
3회로는 그 분산을 추정한 것이지 좁힌 것이 아니다.

`repeat_runs` 는 합산 구간 [53.7%, 64.7%] 을 냈지만, 도구가 스스로 경고하듯
서로 다른 학습은 같은 정책의 표본이 아니다. **시드를 늘리는 것만이 이 폭을 줄인다.**

## 조건

`wrist_only_dagger` 와 **모든 것이 같고 시드만 3·4** 다.
`seed_base: 3` · `runs: 2` · 나머지 동일 (데이터·epochs·평가 시드 블록·행동공간·카메라).

`--tag` 가 다르므로 기존 체크포인트를 덮어쓰지 않는다.

## 사전등록 판정 (결과 전 확정)

**주 판정: 5시드 합산.**

| 5시드 평균 | 판정 |
|---|---|
| >= 50% | 손목 전용 결론 유지. `count: 1` (D-AI-39) 확정 |
| 35 ~ 50% | 3시드 평균 59.3% 가 운이 좋았던 것이다. 여전히 대조군(33.3%)보다는 위 |
| < 35% | **3시드 결과를 신뢰할 수 없다.** D-AI-39 를 보류하고 원인부터 찾는다 |

**보조 판정**

1. 5시드 범위 폭이 33%p 보다 **넓어지면** → 분산을 과소추정했던 것이다.
   "배포 가능" 판정을 유보하고 n 을 더 올린다
2. 새 두 시드가 둘 다 45% 미만이면 → 기존 seed2(78%)가 이상치였을 가능성.
   최고·최저를 뺀 3시드 평균을 함께 보고한다
3. 대조군(2대)과의 합산 구간이 **여전히 겹치지 않으면** → +26%p 결론은 유지된다

## 예측 (실행 전) 🟡

- 5시드 평균 **52~62%** (3시드 59.3%보다 약간 내려간다 — 3개 표본의 평균은 위로 튀기 쉽다)
- 범위 폭은 **비슷하거나 약간 넓어진다** (35~45%p). 표본이 늘면 극단값이 더 나온다
- 대조군과의 구간 비겹침은 **유지**된다

## 해석 제한

- 여전히 시뮬이다
- 5시드도 분산 추정으로는 적다. 폭이 좁아지는 것은 **평균의 구간**이지 개별 체크포인트가 아니다
- 대조군(2대)은 3시드 그대로다. 시드를 한쪽만 늘리면 비교가 비대칭이 된다 —
  **결론을 뒤집는 용도가 아니라 손목 전용 쪽 평균을 단단히 하는 용도로만 읽는다**
