# MEASURE — hand-eye 물리검증과 재촬영의 기여도 분리 (2026-09-21)

작성 김준태 (트랙 B) · 계측기 `AI/tools/audit_umi_zarr.py` (자체검증 12/12)
산출물 `out/gate_20260921.json`

## 왜 이걸 쟀나

2026-09-20 에 어시스턴트가 **"hand-eye 를 물리 측정하니 위치 오차가 41.6 → 24.4mm 가 됐다"**
고 말했다. 그때 *"두 배치는 편 수·시연 길이도 달라 교란이 있다"* 는 단서를 달았지만,
그 교란을 가르지 못한 채 hand-eye 효과로 읽힐 수 있는 문장이었다.

2026-09-21 에 `s22_pick_20260921_atlas_v1` 이 나오면서 **교란이 갈렸다.**
이 배치는 `camera_tcp_status: physically_validated` 인데 **원본은 옛 수집분**이다.
즉 hand-eye 만 바뀌고 데이터는 그대로인 비교군이다.

## 이 배치가 옛 수집분이라는 근거 🟢

이름은 `20260921` 이지만 report 의 `slam_tag` 가 옛 매핑 편을 가리킨다.

```
slam_tag  .../s22_pick_20260921/processed_mapping/rec_1789701760510_41f56c1e/tx_slam_tag.json
```

`rec_1789701760510_41f56c1e` 는 v4 가 쓴 2026-09-11 매핑 편이다 (우리 로컬
`s22_umi_v3/processed_mapping` 에 같은 것이 있다). 감사 수치도 v4 와 사실상 같다.

| | v4 | 20260921 |
|---|---|---|
| 편 / 프레임 | 76 / 16328 | 70 / 15161 |
| 경로/직선 비 중앙 | 2.90 | **2.89** |
| 편 길이 중앙 | 215.5 | **217.5** |
| 편당 이동거리 중앙 | 1.416 m | **1.413 m** |
| 편별 span 중앙 | 0.488 m | **0.483 m** |

## 결과 — 기여도가 갈린다 🟢

| 배치 | camera_tcp | 원본 | 위치 거리 RMSE | hold 대비 |
|---|---|---|---|---|
| v4 | `cad_estimate_axis_mapping_unvalidated` | 옛 92편 | 41.60 mm | 3.52× |
| **20260921** | **`physically_validated`** | **옛 92편** | **39.71 mm** | 3.67× |
| s22_pick_v3 | `physically_validated` | **재촬영 35편** | **24.39 mm** | 3.12× |

```
hand-eye 물리검증   41.60 → 39.71  =  1.9 mm   (데이터 고정)
재촬영             39.71 → 24.39  = 15.3 mm   (camera_tcp 고정)
```

**개선의 8할이 재촬영에서 왔다.** hand-eye 물리검증은 1.9mm 다.

## 2026-09-20 진술 정정

어시스턴트가 "hand-eye 가 41.6 → 24.4mm 를 만들었다"고 말한 것은 **틀렸다.**
그 차이의 대부분(15.3mm)은 시연을 다시 찍어서 생겼다. 정정한다.

## 게이트 판정과 사전 예측

`--gate --rate-hz 30.0021` 로 판정: **통과 5 · 불합격 4 · FAIL**

| 항목 | 값 | 기준 |
|---|---|---|
| 편 수 70 · 결측 0/15161 · 닫힘 70/70 · 개구 폭 1.56mm · 표본율 30.0021Hz | | 통과 |
| 편 길이 중앙 | 217.5 프레임 | 60~150 ✗ |
| 경로/직선 비 | 2.89 | ≤2.5 ✗ |
| EEF 축별 최대 폭 | 0.466 m | ≤0.30 ✗ |
| 회전 크기 폭 | 0.749 rad | ≤0.35 ✗ |

**결과 보기 전 예측** 🟡 — 옛 92편 기반이면 경로/직선 2.5 초과, 회전 폭 0.35 초과,
편 길이 중앙 150 초과로 FAIL. 참조집단 v4 (2.90 · 0.519 · 215.5).

**빗나간 방향**: 세 항목 다 맞았으나 **회전 폭이 예상보다 컸다** (v4 0.519 → 0.749).
같은 원본인데 회전 폭만 44% 커진 이유는 미확인 — atlas localization 이 자세 추정을
바꿨을 가능성이 있으나 **측정하지 않았다.**

## 내일 계획에 미치는 영향

바꾸지 않고 **강화한다.**

- 재촬영이 주효과다. 게이트가 옛 배치를 떨어뜨린 이유(경로/직선 2.89 · 회전 폭 0.749)가
  곧 그 15.3mm 다
- 촬영 조건을 지키는 것이 hand-eye 보정보다 8배 크다: 편당 2~5초 · 경로/직선 ≤2.5 ·
  시연자 한 명 고정 · 접근 방향 고정
- hand-eye 는 여전히 필요하다 (1.9mm + 상수 편향은 학습이 못 지운다). 다만
  **그것만으로는 24mm 가 안 나온다**

## 한계

- 세 배치의 편 수가 다르다 (76 / 70 / 35). 표본 크기 교란이 남아 있다
- 전부 **개루프 예측 오차**다. 롤아웃 성공률이 아니다
- 회전 폭 증가(0.519 → 0.749)의 원인 미측정
- `physical_deployment_ready: True` 인 배치가 생겼지만 **실물 검증은 미실행**이다
  (모터·그리퍼 고장)
