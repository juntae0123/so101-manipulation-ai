# PREREG — M0 enum 설명 v2 (2026-09-12)

작성자: 김준태(트랙B) · 이슈 S15P21A103-36 · `PREREG_vlm_m0_0912.md` 의 후속

## 이건 게이트 변경이 아니다

**v1 측정(`MEASURE_vlm_m0_0912.md`)은 그대로 남는다.** 결과를 보고 기준을 옮기는 것이
아니라 **다른 조건**을 새로 재는 것이다. 두 판의 결과 파일명·EXP_LOG 조건에
`prompt_variant` 를 넣어 섞이지 않게 했다.

## 왜

v1 실측 🟢: `sort_two` 0.35, 오분류 **12/20 이 전부 `pick_place` 방향**.
`align_fixture` 2건 · `present_inspect` 4건 · `line_up` 1건도 같은 방향이다.
**오분류가 한 값으로 쏠린다** — 무작위 혼동이 아니다.

v1 의 enum 설명은 D-AI-22 의 이름을 그대로 썼다: `pick_place: 집어 옮기기`.
그건 나머지 넷의 **상위 개념**으로 읽힌다 — 분류도 정렬도 검사 제시도 집어 옮기는 일이다.

**프롬프트로 고쳐지는 것을 가중치로 고치면 무엇이 고쳐졌는지 귀속시킬 수 없다.**
LoRA 판단(D-AI-40) 전에 이걸 먼저 잰다.

## 조건

v1 과 **모든 것이 같고 enum 설명만 다르다.** 지시문 100개·이미지 조건·모델·시드 동일.

v2 는 각 항목에 **서로를 배제하는 특징**을 넣는다 — 목적지가 하나인가 둘인가,
자세를 맞추는가, 제자리로 돌아오는가, 여러 개인가.

## 사전등록 판정 (결과 전 확정)

**주 판정: `sort_two` 정확도.**

| v2 의 `sort_two` | 판정 | 다음 |
|---|---|---|
| >= 0.70 | **프롬프트 문제였다** | enum 설명을 v2 로 확정. D-AI-24 스키마에 설명문을 포함시킨다 |
| 0.50 ~ 0.70 | 부분적 | 설명을 더 손볼 여지가 있으나 수익이 준다. 여기서 멈춘다 |
| < 0.50 | **프롬프트 문제가 아니다** | 지시문 자체가 모호하거나 모델 한계다. 사람이 다시 쓴 지시문 판으로 재측정 |

**보조 판정**

1. 전체 정확도가 v1(0.75)보다 **떨어지면** → v2 가 다른 스킬을 망가뜨린 것이다. v1 을 유지한다
2. `pick_place` 가 1.00 에서 떨어지면 → 설명을 좁힌 대가다. 전체가 올랐으면 받아들인다
3. `render − blank` 는 여전히 0 에 가까울 것이다 — 그건 프롬프트로 바뀌지 않는다.
   **0.10 이상으로 벌어지면 v1 의 결론(이미지 기여 없음)을 다시 본다**
4. SmolVLM-500M 의 상수 예측기 붕괴가 v2 에서 풀리면 → 경량 후보가 되살아난다 (이슈 42)

## 예측 (실행 전) 🟡

- `sort_two` **0.55~0.80** — 오분류가 한 방향으로 쏠린 것은 설명이 겹친다는 신호이므로 오를 것이다.
  다만 "판별"이라는 개념 자체가 지시문에서 늘 명시되지는 않아 1.00 은 안 될 것이다
- 전체 정확도 **0.80~0.90**
- `render − blank` 는 **여전히 0.05 미만**
- **SmolVLM-500M 은 여전히 붕괴할 것이다** — 상수 예측기는 설명 문구로 풀리는 종류가 아니다

## 해석 제한

- 지시문 100개는 어시스턴트가 썼다. v1 과 같은 제한이 그대로 적용된다
- **v2 설명도 내가 썼고, v1 결과를 본 뒤에 썼다.** 즉 이 판은 이 테스트셋에 유리하게
  조정됐을 수 있다. 사람이 다시 쓴 지시문 판에서 재측정하기 전까지
  v2 수치를 "프롬프트를 고치면 이만큼 된다"의 일반값으로 쓰지 않는다
- n=100 · 95% 구간 반폭 약 ±9%p
