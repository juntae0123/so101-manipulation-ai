# LIMITS — 미검증 항목

**여기 있는 것은 "아직 모른다"는 뜻이다.** 발표·보고에서 이 항목을 사실처럼 말하면 안 된다.
검증되면 항목을 지우고 근거 문서를 링크한다.

기준일 2026-08-31 · 작성 김준태(트랙 B)

## 치명적 — 이게 안 풀리면 제품 주장이 성립하지 않는다

| # | 미검증 항목 | 어떻게 풀리나 |
|---|---|---|
| L1 | **학습 정책의 롤아웃 성공률이 하나도 없다.** BC 는 랜덤 텐서로 루프만 완주했다 | 실데이터 학습 → `tools/eval_rollout.py --policy-ckpt` |
| L2 | **실물 검증 0건.** 모든 수치가 시뮬이다. 시뮬 성공률이 실물을 보장하지 않는다 | 실물 도착 후 재측정. 시뮬 수치는 그때 **상한 추정치로 강등** |
| L3 | **이미지-액션 타임스탬프 오차 미계측.** 시뮬은 구조적으로 0이라 잴 대상이 없다 | 이슈 30. 실물 수집 **전에** 반드시. 검증기는 10ms 초과를 위반으로 잡지만 그 10ms 의 근거가 약하다 |
| L4 | **스크립트 시연으로 학습한 정책이 실물로 가는지 모른다.** 스크립트는 특권정보(물체 좌표)를 쓰고 정책은 이미지만 본다 — 이 차이가 학습을 정당화하지만, 실물 전이는 별개다 | 각 스킬마다 replay baseline 동시 측정 + 실물 도착 후 대조 |

## 계약·인터페이스

| # | 미검증 항목 | 비고 |
|---|---|---|
| L5 | 데이터 계약 3건 미확정 — 정규화 기준(`new_calib` 고정 여부) · **범위 초과값 처리** · 카메라 구성 | 트랙 A 합의 대기. 28·29·30·31·34·64 를 막고 있다 |
| L6 | 실물 서보 캘리브레이션이 `new_calib` 인지 미확인 | HW 확인 필요. 아니면 **수집 데이터 전량 무효** |
| L7 | 224×224 해상도로 1.5~2.5cm 물체의 bbox grounding 이 되는지 | VLM 되살릴 때 측정 (R-AI-4) |
| L8 | 각도 규격 통과(1.37e-06 < 1e-4)는 **허용오차가 막아준 것**이지 규격 일치가 아니다 | 로봇팔 변형 시 재측정 · `docs/MEASURE_hw_angles_0828.md` |

## 배포·하드웨어

| # | 미검증 항목 | 비고 |
|---|---|---|
| L9 | **Jetson 8GB 에 VLM+정책이 올라가는지 미검증** | 이슈 42 |
| L10 | bf16 학습 어댑터를 4bit 양자화 베이스에 붙였을 때의 품질 손실 | llama.cpp 는 양자화 인지 학습을 하지 않는다 🔵. 경로 A/B/C 실측 필요 |
| L11 | **GPU 는 Tesla V100 32GB × 10대(320GB)다.** V100 에서 VLM LoRA 를 한 번도 실행하지 않았다 | `docs/RESEARCH_vlm_v100_0831.md` |
| L26 | **10장의 구성을 모른다** — 한 노드인가 여러 노드인가, NVLink 인가 PCIe 인가 | M7. 스케일링 예측이 여기 걸린다 |
| L27 | 10잡 동시 실행 시 잡당 처리량 저하율 미측정 | M8 |
| L28 | **UMI 수용률(폐기율) 미측정.** 5종 × 150건 수집 소요시간이 이 숫자에 비례한다 | 파일럿 20건으로 역산 (S15P21A103-113) |
| L19 | **후보 VLM 이 전부 bf16 체크포인트인데 V100 은 bf16 연산이 없다.** fp16 캐스팅 시 inf/NaN·gibberish 사례가 문서화돼 있다 🔵 | 착수 전 M1 계측 (fp32 vs fp16 argmax 일치율 ≥99%) |
| L20 | V100 에서 SDPA mem-efficient 커널의 실제 속도 (FA2 대비 정량 비교 불가) | 미측정 |
| L21 | Jetson Orin Nano 8GB 에서 OS·ROS2 가 점유하는 실제 메모리 | M5 의 "여유 2GB" 는 🟡 설계 판단 |
| L22 | 시뮬 렌더 이미지로 학습한 VLM grounding 이 실사에서 되는지 | 도메인 갭 미측정. 불량 판정은 시뮬로 렌더 자체가 불가 |
| L23 | UMI 마커 포즈 정확도 (W1 게이트 5.7mm) 미계측 | 폰을 팔로워 손목에 고정, 로봇 FK 를 정답으로 대조 |
| L24 | UMI 궤적의 **재현 불가 폐기율** — SO-101 은 팔이 5자유도다 🔵 | 수집 중 실행가능성 검사 필요. 검사기 미구현 |
| L25 | **리더 암이 실제로 오는지 미확인.** 리더-팔로워는 물리적으로 팔이 2대다 | HW 확인 필요. 없으면 보험 라인이 성립하지 않는다 |
| L12 | 30Hz 실시간 예산(33.3ms) 안에 실물 추론이 들어가는지 | 시뮬 텔레옵 녹화에서 이미 519ms/틱으로 15배 초과한 사례가 있다 🟢 |

| ~~L29~~ | ~~ARCore pose 스트림과 RGB 스트림 사이 타임스탬프 오프셋 미계측~~ **해소 (2026-09-07)** 🔵 — MW 실측: pose 와 이미지가 같은 ARCore 프레임에서 나오고, 에피소드 2건에서 `timestamp_ns` 집합이 1:1(361/573행, 유니크·단조증가, 드롭 0). 남는 리스크는 IMU↔pose 약 7ms (30Hz=33ms 대비 작다). 이전 내용: 마커 방식일 때 구조적 0 이던 것이 D-AI-21 로 되살아났다 | 수집 착수 전 필수 (S15P21A103-30) |
| L30 | **hand-eye `T_cam→tcp` 실측값 없음.** 오차가 궤적 전체에 상수 편향으로 들어간다 | 게이트 제안 5점 평균 ≤5mm / 최대 ≤10mm. 실측상 ±10mm 에서 재생 3/4 🟢 |
| ~~L31~~ | ~~자유도 불일치~~ | **해소 (2026-09-02, HW 김현석).** "6축"은 **모터 6개**를 뜻한다 = 팔 5축 + 그리퍼 개폐. 실측·계약과 일치한다. **다만 팔의 공간 자유도가 5인 것은 확정**이므로 사람 손목 6DOF 궤적의 재현 불가 비율은 여전히 실측 대상이다 (L24) |
| L32 | ARCore SLAM 포즈 드리프트 (세션 길이 누적) 미계측 | 로봇 FK 를 정답으로 대조 |
| L33 | 촬영 기기 확정 미확인 (기획서 Galaxy S23, 이전 기록 S22) | 카메라·IMU 사양이 다르면 캘리브 값이 다르다 |
| L34 | **조건 B 외란 크기가 실제 환경 변동과 맞는지 미검증** | D-AI-18 붕괴율의 전제. 🟡 설계 판단이며 계측값 아님 |

| L35 | **계약 0.2.0 전환으로 `sim_pick_v1`(98편)과 `bc_sim_pick_v1.pt` 무효** | 재수집 필요. 어제까지의 측정치는 0.1.0 조건에서 유효하며 인용 시 버전 병기 |
| L36 | **D-AI-26 은 트랙 A 대행 결정이다** (황도경 FE 작업 중) | 복귀 시 공유·재확인 필요 |
| L38 | **계약이 같아도 `state` 의 의미가 시뮬과 UMI 에서 다르다.** 시뮬 `state` 는 물리 적분 후의 실제 관절각(서보 지연·중력 처짐 포함)이고, UMI 시연에는 로봇이 없으므로 `state = IK(pose)` — 동역학이 없는 이상적 기구학 해다. 포맷은 일치하고 **분포는 다르다** | D-AI-29 의 시뮬→UMI 전이가 여기에 걸린다. 계측 제안: 같은 궤적을 IK 로 합성한 state 와 시뮬 실행 state 로 각각 만들어 관절별 차이를 잰다. 미착수 |
| L39 | **정책이 1계조 픽셀 노이즈에 결정을 뒤집는다** 🟢 — 렌더 최대 픽셀차 1.0(255 중 1)에서 롤아웃 성공/실패가 갈리고 그리퍼 닫는 시점이 74틱→37틱으로 옮겨진다. 실물 카메라 노이즈는 1계조보다 훨씬 크다 | 실물 전이의 조기 경보다. 강건성 대책 전에 렌더 결정성부터 확보해야 판정이 가능하다. MEASURE_closing_moment_0903 |
| ~~L37~~ | ~~롤아웃 평가가 재현되지 않는다~~ | **해소 (2026-09-03).** 원인 = 오프스크린 MSAA(`offsamples=4`). `cameras.offsamples: 0` 으로 끄자 R·P·E 비트 동일 🟢 (같은 시드 두 번: 닫은틱 77/77, 최소거리 3.82/3.82). 수집·평가 모두 0 유지. 이전 기록: `check_determinism`: R 렌더 **다름**(1계조) / P 추론 동일 / E 롤아웃 다름 → **렌더가 비결정적이다**. 대책: 서버에서 `MUJOCO_GL=osmesa` 검증 (클라우드 osmesa 는 R·P·E 전부 통과 🟢). 강건성 쪽 함의는 L39 |
| L37-old | **롤아웃 평가가 재현되지 않는다** — 같은 ckpt·같은 시드로 11/10/7 (n=100 ×3) 🟢. 이미지를 쓰지 않는 정책(hold·zero·scripted)은 완전히 재현되므로 원인은 이미지 경로다 | 프로토콜: 3회 평가 + 합산·범위 보고. 원인 미확정 — `check_determinism` 필요. MEASURE_eval_nondeterminism_0903 |

## 제품·시장

| # | 미검증 항목 | 비고 |
|---|---|---|
| L13 | **타겟 도메인의 지불 의사와 시장 규모를 조사하지 않았다** | 발표에 수치로 쓰려면 조사 필요. 지어내면 안 된다 |
| L14 | 후보 도메인의 실제 대상물이 1.5~2.5cm / 수십 g 안에 들어오는지 | 사진 몇 장으로 판정 가능 |
| L15 | FLY ASIA 심사 기준(시장성 vs 완성도) 미확인 | 도메인 선택의 절반이 여기 걸려 있다 |
| L16 | 다품종 소량 현장에서 외관 검사를 사람이 하고 있는지 | 제조업 종사자 1명에게 물으면 확인됨 |

## 계측 인프라

| # | 미검증 항목 | 비고 |
|---|---|---|
| ~~L17~~ | ~~`EXP_LOG` 의 모든 기록이 `git_rev: unknown`~~ | **해소 (2026-09-01).** 서버 체크아웃 안에서 계측이 돌면서 실제 git_rev 가 기록된다 |
| L18 | 양팔·다른 구조에서의 학습 정책 없음 | 구조 확장 데모는 스크립트 구동까지만 주장한다 (D-AI-13) |

## 판정 가능성 (2026-09-07 추가)

| # | 미검증 항목 | 비고 |
|---|---|---|
| L40 | **학습 실행 간 편차 12%p 가 게이트 폭 20%p 와 같은 급이다** | v5 실측 🟢 2 / 12 / 14%. 같은 데이터·같은 설정·시드만 다름. 편차를 줄이는 것이 성공률을 올리는 것과 같은 급의 과제다. **정정 (2026-09-07):** 이전 비고에 "v2(5.7%)·v3(4.3%)·v5(9.3%) 구간이 전부 겹친다"를 편차의 증거로 적었는데, 그 셋은 **같은 데이터**(state+action 해시 동일)에 코드·config 가 다른 실행이었다. 조건 간 겹침은 실행 편차의 증거가 아니다 — TS_provenance_holes_0907 |
| L41 | ~~`closing_moment` 가 연속 출력 정책에 무효~~ | **해소 (2026-09-07).** 중간값 통과 → 하위 25% 문턱을 5틱 유지로 교체. 옛 정의는 bc seed3000 을 틱 60 으로 찍었으나 실제는 102 였다 (42틱 오차). 7개 사례 하네스로 검증 |
| L42 | **폐루프 발산 곡선이 에피소드 1편에서만 측정됐다** | `trace_execution` C 절: state 차가 매 10틱 약 2배 (t=1 0.000718 → t=20 0.015796 → t=40 0.148242). 다른 에피소드에서 같은 지수인지 미확인. n=1 |
| L43 | 그리퍼 명령 원계열 확인이 **시드 2개**뿐 | seed 3000·3007 에서 "제대로 잡고 들어올리지 못함"을 확인. 100편 전체 분포는 미확인 |
| L45 | **`git_rev`·`code_sha` 가 로그 시점에 계산된다 — 다른 계측 도구 미적용** | `log_run` 이 리비전을 기록 시점에 잰다. 몇 시간 도는 잡에서는 그 트리가 수치를 만든 트리가 아니다 🟢 — v6 이 잡 시작 시점에 없던 커밋 `b7ffabe` 를 자기 리비전으로 기록했고, `train_config_sha` 가 None 인 것도 로드된 모듈이 그 필드보다 먼저였기 때문이다. `eval/repeat.py` 만 `CODE_SHA_AT_LAUNCH` 로 고쳤고 `eval/rollout.py`·`tools/*` 는 그대로다 |
| L46 | **v3→v5 의 4.3%→9.3% 를 어느 변경에도 귀속시킬 수 없다** | 데이터는 바이트 동일인데 `code_sha`·`config_sha` 가 둘 다 다르고 `dirty=True` 였다 🟢. 학습 비결정성은 아니다(코드가 실제로 달랐다). 단일 요인 귀속 불가 — D-AI-31 로 프로세스를 바꿨으나 **이미 나온 수치는 복원 불가** |
| L47 | **정지 창 22틱의 인과가 롤아웃으로 확인되지 않았다** | `audit_demos` 🟢 22틱(Q1=Q3=22, 98편 전부), v4 37틱 0/300 과 v5 22틱 9.3% 라는 두 점이 용량-반응과 부합한다. 그러나 **창을 줄이면 오른다**는 예측은 미검증이고, 두 점 다 3회 실행이라 실행 수준 p=0.05 경계다. kick(고정점 검사)과 Z1(재표집)이 미실행 |
| L48 | **obs/action 페어링이 코드 읽기로만 확인됐다** | `data/collect.py` 의 ctrl 설정 → capture → mj_step 순서로 `state_t` 가 `action_t` 적용 전 관측임이 보장된다 🟡. `probe_contract` 로는 검사 불가 — 편차 지표가 shift 와 무관하고, 한 틱 지연은 위치 제어가 흡수한다 🟢. 런타임 검증(sentinel action)은 실물·UMI 경로(S15P21A103-30)에서 필요 |
| ~~L49~~ | ~~달성 상승량이 51mm 에서 포화 / 성공 기준이 물리 능력의 98% 지점~~ | **철회 (2026-09-07, 같은 날).** 계측 산물이었다 — `rollout`/`probe` 가 `is_success()` 에서 끊고 `lift_height()` 를 그 뒤에 읽으므로, 성공한 편의 상승량은 **판정선 통과 순간의 높이**다. `stop_on_success=False` 로 재측정: 개루프 달성이 명령의 **88~91%** 로 일정하다 (5.5→48.1 / 6.0→53.1 / 8.0→**73.2mm**) 🟢. 포화 없음. 능력 문제 없음. 6.0cm 여유 3.1mm, 피드백 전문가 8.1mm. 상세·철회 경위는 MEASURE_demo_lift_margin_0907 §6 |
| ~~L55~~ | ~~롤아웃 성공률이 BLAS 스레드 수에 의존할 수 있다~~ | **해소 (2026-09-07). 스레드 수는 무해하다** 🟢 — `AI_THREADS` 1 과 8 이 12/100·동결 13·파지후동결 6·kick 5/6 까지 **완전히 동일**했다. 캡을 씌운 것이 재현성을 해치지 않는다. 11/100 의 원인은 스레드가 아니라 **CPU 추론**이었다 (그 잡은 `--device` 도입 전 코드라 `BCPolicy` 기본값 cpu). 11 vs 12 는 장치와 코드가 같이 바뀌어 귀속 불가지만 **에피소드 1편 차이**로 모든 구간 안이다 → L57 로 이관 |
| L57 | **추론 장치가 롤아웃 결과를 바꾼다 — 그리고 v2~v6 전부 CPU 였는데 기록에 없다** | 🟢 `probe_freeze` 단일요인 비교 (같은 코드·시드 3000~3099·n=100): `--device cpu` **11/100**, `--device cuda` **12/100**. 스레드는 무관 (1==8 완전 동일). 즉 장치가 유일한 원인이고 코드 변경은 무관하다. 차이가 왜 생기는지는 미확인 🟡 (부동소수점 차이가 폐루프에서 증폭된 것으로 보이나 계측 안 했다). `build_policies` 기본값이 `cpu` 라 v2~v6 의 모든 BC 롤아웃이 CPU 였고 기록 어디에도 그 말이 없었다. 조치: `--policy-device` 추가·`conditions` 기록·**기본값 cpu 유지**로 비교선 보존, `repeat_runs` 가 자식에게 전달 |
| L58 | **파지 후 동결 고정점 판정이 장치에 따라 뒤집힌다 — 🟡 유지** | 🟢 같은 체크포인트·같은 시드에서 cpu n=4 (무개입 0/4 CI 0~49.0 vs kick 3/4 CI 30.1~95.4, **겹침**) / cuda n=6 (0/6 CI 0~39.0 vs 5/6 CI 43.6~97.0, **비겹침**). n=4~6 의 소표본 불안정이고, 범위 좁히기도 사후 변경이다. **독립 확인 필요**: 미사용 시드 블록 3100~3199, 사전등록된 판정 규칙은 MEASURE_freeze_kick_0907 §파지후동결. 어느 쪽이든 기여 상한은 약 6%p |
| L56 | **BC 동결의 다수(7/11)는 파지조차 하지 않은 것이다** | `probe_freeze` n=100 🟢 동결 시 턱접촉 분포 {0: 7, 8: 4}. 공중에서 그리퍼를 닫고 멈춘다 — 파지 후 동결이 아니고 들기 밀기로 도울 수 없다. 실제 파지 후 동결은 4편(4%). 첫 실행이 둘을 합산해 kick 효과를 희석했다(계측기 수정됨). "공중에서 닫는다"가 접근 발산과 같은 뿌리인지 미확인 🟡 |
| L59 | **pose 원점이 에피소드마다 다르다 — 관절각 변환이 불가능하다** | MW 🔵: 세션 시작 = 원점, Y = 중력반대. 계약은 관절각인데 `T_ARCore월드 → 로봇베이스` 가 에피소드마다 미지다. 상대궤적만으로는 로봇이 어디를 향할지 알 수 없다. 방안 A(작업대 ArUco 보드, UMI 방식) / B(시연 시작 자세 고정 — 그 오차가 상수 편향이 되고 실측 ±10mm 에서 재생 3/4 🟢). **A 권고** 🟡. **좌표 변환이라 트랙 A(황도경) 소관 — 별도 확인 필요** |
| L60 | **핸드헬드 핑거가 SO-101 의 50% 축소다 — 시각 도메인 영향 미확인** | MW 🔵: 핑거 50% 축소, 마커만 16mm 원본, 물리 범위 0~90mm. `gap_m` 매핑(`[0,90]→SO-101`)은 문제없다. 문제는 손목 카메라에 찍히는 핑거의 겉보기 크기다 — UMI 가 캘리브레이션 없이 성립하는 근거가 그 일치다 🟡. **카메라→핑거끝 거리도 절반이면 상쇄된다.** 필요한 숫자 둘: 핸드헬드의 카메라→핑거끝 거리, 로봇의 예정 거리(HW). 비가 0.5 에서 멀면 학습 후 실물에서만 드러나는 실패다 |
| L52 | **UMI `gap_m` 의 프레임 위치 의존성 미계측** | 16mm 마커를 프레임별 스케일 기준으로 쓰는 방식이므로 반경 왜곡이 겉보기 크기를 바꿀 수 있다. ARCore 는 왜곡계수를 주지 않는다 🔵. 판정 기준 ±2mm 를 결과 전 확정. 추가로 어림 🟡: 640px/74° 에서 300mm 거리 1px≈0.6mm, 16mm 마커≈26px, ±1px 검출오차 → 90mm 게이지에 ±3.5mm — 왜곡과 무관하게 기준을 못 넘을 수 있다. MW 스윕 대기 |
| L53 | **UMI 핸드헬드 개구 범위가 SO-101 과 다르다** | MW 캘리브 0~90mm 🔵 vs 공식 MJCF 팁 간격 4.1~133.4mm 🟢. 핑거 STL 을 그대로 쓴 설계였다면 같아야 한다. 범위가 다르면 `gap_m` 을 로봇 명령으로 바로 못 쓰고 매핑이 필요하다. MW 회신 대기 (핑거 형상 / 캘리브 상한 / 측정 지점 중 무엇인가) |
| L54 | **~~L29~~ 해소 근거 확인 대기** | MW: pose 와 이미지가 같은 ARCore 프레임에서 나와 `timestamp_ns` 가 동일하므로 pose↔frame 시차가 구조적으로 0 이다 🔵. 확인 방법: 한 에피소드에서 `poses.csv` 와 `frames.csv` 의 타임스탬프 집합이 정확히 같은지. 같으면 L29 를 내린다. 남는 리스크는 IMU↔pose 약 7ms (30Hz=33ms 대비 작다). 첫 프레임 −47~−68ms 가 상수 편향인지 지터인지도 미확인 |
| L51 | **서버 사용 규약이 도구에 내장돼 있지 않다** | SSAFY 원칙은 **GPU 한 장 할당·잡 1개**다. 어시스턴트가 D-AI-30 비용을 10장 독점으로 계산했고(1차 정정 2~3장, 2차 정정 1장), 실제로 `CUDA_VISIBLE_DEVICES=1` 로 두 번째 장을 잡아 서버 측 경고를 받았다 🟢. 추가로 프로세스마다 BLAS 스레드를 코어 수(80)만큼 띄워 6개 프로세스에서 load average 165/80 이 나왔다. **`CUDA_VISIBLE_DEVICES` 한 장 + `OMP/MKL/OPENBLAS_NUM_THREADS=2` + 중복 실행 방지(락파일)를 도구에 내장해야 한다** — 매번 손으로 붙이면 빠뜨리고, 빠뜨리면 남의 작업이 죽는다. 미착수 |
| L50 | **천장 손실 16%p 가 들기 IK 도달 불가임을 확인했으나 `lift_height` 변경은 미적용** | n=100 🟢 8cm IK실패 15 ↔ 실패 16, 6.0~7.0cm IK실패 0 ↔ 실패 1. 명령 0.08→0.06 이면 84%→99% (구간 비겹침). **시연자가 바뀌므로 재수집이 따라오고 `old-expert 400` 대조군 계획과 충돌한다** — 현행 시연자 버전을 먼저 고정해야 한다. 6.0/6.5/7.0 이 전부 99/100 이라 그 안에서는 구분 불가, 여유로 고르면 7.0cm 이 나을 수 있다 🟡 |
| ~~L44~~ | ~~`bc.yaml` 기본 `epochs: 20` 이 실험값 30 과 다르다~~ | **해소 (2026-09-07).** 기본값을 30 으로 맞췄다. | 이전 비고: `--epochs` 를 빼먹으면 조용히 비교 불가능한 결과가 나온다. 표류 감지기를 넣었으나(66c1afd) **기본값 자체는 아직 안 맞췄다** |

## UMI 관통 (2026-09-08~09 추가 · 트랙 A 대행분 · 작성 김준태)

> **번호 재배정 기록.** 이 아래 L61~L66 은 2026-09-08 에 **L40~L45 로 적었던 것**이다.
> 그 시점 작업 트리가 `ai` 보다 35커밋 뒤처져 있어 로컬 파일을 세서 "최대 39" 로
> 판단했다. 실제 `ai` 는 L60 까지 썼고 L40~L45 는 시뮬·정책 쪽 항목이 차지했다.
> 그 커밋(`a69a297`)은 rebase 에서 skip 됐고, 여기서 **L61~L66 으로 다시 올린다.**
> 규칙: 번호는 세지 말고 `origin/ai` 를 조회한다 (`claude/규칙_정정_Git_MR.md`).
> 문서 안의 예전 `L40~L45` 표기(`track_a/HANDOVER_track_a.md` §9,
> `track_a/calibration/__init__.py`)는 이 표로 읽는다.

| # | 미검증 항목 | 비고 |
|---|---|---|
| L61 | **계약이 이미지 비교가능성을 보장하지 않는다** | `EpisodeMeta.cameras` 는 이름 목록뿐이고 fovy·왜곡계수·마운트 pose 를 담을 필드가 없다 🟢. 기하가 다른 두 데이터셋이 같은 이름으로 계약을 통과한다. 시뮬 `cam_wrist` 는 fovy 70, 실물 폰은 미측정. **실데이터 0편인 지금이 가장 싼 구간이다** — 750편 모은 뒤에는 전량 폐기다. 계약 확장은 양 트랙 합의 + D-AI 사안. 내가 붙였던 처방("제3의 정규 가상 카메라로 양쪽 재투영")은 **철회했다** (REJECTED, 2026-09-09) — 한계는 그대로 남는다 |
| L62 | **`cam_front`(3인칭)의 UMI 시연 측 대응물이 정의돼 있지 않다** | 계약은 카메라 2대다. UMI 핸드헬드에는 손목 카메라만 있다 🔵. 시연 때 3인칭을 따로 세우는지, 아니면 실물 학습은 1대로 가고 계약을 바꾸는지 미결. 어느 쪽이든 시뮬 데이터의 유효성에 영향. **해소 (2026-09-11) 🔵 사용자 구두 확인: 실물 카메라는 UMI 그리퍼 1대 + 로봇팔 1대이고 같은 각도다 — 즉 같은 시점을 두 번 구현한 것이며 동시에 켜지지 않는다. 3인칭 카메라는 실물에 존재하지 않는다.** `cam_front` 는 '대응물 미정의'가 아니라 **대응물 없음**으로 확정. 남는 것은 마운트 위치·FOV 정합(미확정)이고, 배포 구성 불일치는 L76 으로 분리한다 |
| L63 | **수용률 곡선이 `max_iters=120` 에서 측정됐다** | `MEASURE_umi_reach_0907` R1~R6 🟢. 이후 `MujocoIK.max_iters` 를 500 으로 올렸는데 곡선을 다시 재지 않았다. 각도 5도·위치 20mm 라는 90% 수용 지점이 iters 상한의 산물일 수 있다. 재측정 비용 낮음 |
| L64 | **첫-스텝 다중시작 시드 8개의 충분성 미검증** | `_seed_grid()` 는 결정적 8개다 🟢. 이 격자로 100% 수용이 나온 것은 합성·시뮬 유래 pose 에서다. 실기록 pose 분포에서 8개가 부족하면 조용히 수용률이 떨어진다 — 실패가 "IK 미수렴"으로만 보이고 원인이 시드라는 표시는 없다 |
| L65 | **실데이터 수용률 미검증 — 섭동 실험은 도달 가능 pose 를 흔든 것이다** | R1~R6 의 입력은 시뮬 FK 로 만든 pose 에 섭동을 준 것이다 🟢. 즉 **출발점이 이미 도달 가능**했다. 사람 손 궤적은 도달 불가 영역에서 시작할 수 있고 그때 수용률은 이 곡선과 무관하다. ARCore 로그 1건이 오면 즉시 재측정 |
| L66 | **0907~0908 UMI 수치 전부가 비표준 환경에서 나왔다** — 부분 해소 (2026-09-09) | 데스크탑 VM(Python 3.10 + 별도 설치 mujoco), 서버 `aiot_v100` 도 로컬 `aiot_ai` 도 아니다. **대조 결과 🟢:** 정규화 두 구현 최대차 `2.384e-07` 로 두 환경 바이트 동일, IK 위치 오차 `pos_tol=1e-7` 에서 양쪽 모두 tolerance 하한(0.0001mm), 수용률 100%·계약 위반 0 동일. 즉 **기구학·정규화·변환 수치는 환경 무관**이다. **남는 것: 렌더·학습 수치는 대조하지 않았다** — 그쪽은 여전히 환경 의존이고 L37/L39/L57 계열과 같은 축이다 |
| L67 | **실물 실행 API 의 가속 검사가 연속 궤적을 구조적으로 거부한다** | `5.774*dq/dt^2 <= 3.0` 에서 `dq=v*dt` 이므로 검사값 = `5.774*v/dt` — **dt 를 줄이면 검사값이 커진다** 🔵(코드 읽기). 30Hz 25.8x, 1Hz 0.9x. 속도한계를 딱 지킨 시연(v=0.5333)도 `dt>=1.026s` 를 요구한다. 정책 주기 하향도 시연 감속도 해결책이 아니다. 상수(`5.774`·`3.0`·`dt>=0.05`)가 실배포 버전에서 같은지 **HW 확인 필요**. 방안 a/b/c 는 MEASURE_umi_realapi_rate_0909 §4 |
| L68 | **`_longest_ok_run` 이 궤적 수준 실행가능성을 안 본다** | 스텝별 IK 성공만 검사한다 🟢(코드). 인접 두 스텝이 2 rad 차이나도 수용률은 100% 로 읽힌다 — 분기 점프·속도·가속을 보지 않기 때문이다. L67 과 같은 뿌리다. 필요한 것: `dq`·`qdot`·`qddot`·분기 점프 검사 추가. 미착수. **이걸 넣기 전의 수용률 수치는 상한이다** |
| L69 | **계약이 같아도 `action` 의 의미가 두 갈래다** | 같은 `contract_version` 에서 시뮬·정책은 MuJoCo `ctrl`(액추에이터 목표, RMS 4.6987e-02), UMI 변환기는 `q[t+1]`(다음 스텝 도달값, RMS 1.602e-02) 🟢. 2.93배 차이. 두 데이터를 섞으면 라벨이 두 공간에 걸친다. D-AI 초안 `DECISIONS_0909_umi_draft.md` · 실험 #1(98편 A/B)이 이 축. **양 트랙 합의 전까지 혼합 학습 금지** |
| L70 | **변환 데이터셋 메타에 원본 raw 경로가 없다 — 문서 수치를 어느 데이터셋이 만들었는지 대조할 수 없다** | `dataset.json` 의 출처 필드가 `"source": "umi_raw"` 처럼 **종류만** 적혀 있고 경로·해시가 없다 🟢. 실제로 0909 정리 중에 문서가 `umi_pick_v4`·`umi_synth_v2`·`v3` 를 참조하는데 디스크에는 없고, 디스크의 최신은 `umi_synth_v9` 인데 어느 문서도 참조하지 않는 상태를 발견했다. **어느 데이터셋이 어느 수치를 만들었는지 사후 확인이 불가능하다.** TS_provenance_holes_0907(학습 실행 쪽)과 같은 결함이 데이터 쪽에서 재발한 것이다. 조치안: `convert_umi.py` 가 `raw_path`·`raw_sha`·`tool_sha` 를 `dataset.json` 에 쓰게 한다. 미착수. **그 전까지 데이터셋 정리는 사람 검토 없이 하지 않는다** |

> **정정 (2026-09-09).** 위 L61~L70 을 올린 커밋 `f46bd86` 의 메시지에
> "`push_both.sh`(CR 63)·`run_transfer_study.sh`(CR 85)가 CRLF 라 리눅스 서버에서
> 죽는다"고 적었다. **틀렸다.** `origin/ai` 블롭은 세 스크립트 모두 `CR=0` 이고,
> 그 CR 은 Windows 작업사본에만 있던 `core.autocrlf` 산물이다. 서버는 원래 LF 를
> 받는다. **작업사본을 재고 저장소 결함이라고 단정했다** — 계측 대상을 틀린 것이다.
> `*.sh text eol=lf` 는 다른 근거(설정 의존 제거)로 유지한다. LIMITS 항목은 아니다.

## 2026-09-10 추가 (김준태·트랙B)

| # | 미검증 항목 | 비고 |
|---|---|---|
| L71 | **`probe_gripper_schedule.py` 의 `verdict` 가 체크포인트 수에 따라 스케일하지 않는다** | `315-319` 행의 `count >= 2` 에서 `count` 는 조건을 통과한 **체크포인트 개수**인데 임계값이 상수 2다 🟢. 체크포인트 1개로 실행하면 최대값이 1이므로 결과와 무관하게 항상 `gripper_only_not_sufficient` 가 출력된다. 2026-09-10 v5 seed0 축소 실행에서 fixed67·75·85 가 사전등록 기준(>20% 이고 learned 대비 >=20%p)을 전부 통과했는데도 그 verdict 가 찍혔다. **1~2개 체크포인트로 돌린 실행의 `verdict` 필드는 무효다.** 조치안: 임계값을 `max(2, ceil(n_ckpt*2/3))` 같은 비율로 바꾸거나, `n_ckpt < 3` 이면 verdict 를 `None` 으로 낸다. 미착수 |
| L72 | **oracle 조건의 폐쇄 판정이 고장 나 있다 — 특권정보 조건이 learned 보다 나쁘다** | DAgger seed0/1/2 에서 oracle 48/21/24 vs fixed60 81/49/38 🟢 (0909), v5 seed0 에서 oracle 3/100 vs learned 11/100 🟢 (0910). 물체 실좌표를 쓰는 조건이 아무것도 안 쓰는 조건보다 낮다. 원인 추정 🟡: `grasp.feedback.grasp_tol_m = 0.005` 가 늦게 만족되거나 아예 만족되지 않아 닫는 시점이 밀린다 — oracle 중앙 close tick 이 fixed 보다 뒤에 있다(70 / 92.5 / 81.5). **함의: 0909 사전등록 판정 2번("oracle 통과 + fixed 실패 → 시각 trigger 필요")은 애초에 검정 불가능했다.** 계측기가 고장이면 그 분기는 영원히 안 켜진다. tolerance 스윕(`PREREG_gripper_oracle_tolerance_0910.md`)을 3일 마감 때문에 폐기했으므로 **미해결.** oracle 수치를 어떤 판정에도 쓰지 않는다. **원인 확정 (2026-09-11) 🟢: v5 seed0 oracle 의 `never_closed` 가 91/100 이다.** `_oracle_ready()` 는 `norm(pinch − (object+grasp_z)) <= 0.005` 인데 정책의 실패 에피소드 최근접 수평거리 중앙이 14.3mm 다 — **정책이 5mm 안으로 들어가지 못하므로 조건이 발동하지 않는다.** oracle 은 '완벽한 폐쇄 시점' 이 아니라 '정책이 5mm 안에 들어오면 닫아준다' 였고, 팔은 여전히 정책이 몬다. 따라서 3/100·48/21/24 는 성능이 아니라 tolerance 미충족률이다. tolerance 확대는 반쪽 처방이다 — ±15mm 에서 재생 성공률이 1/4 이라(🟢) 발동시켜도 대부분 놓친다. 조치: 정의를 '그 에피소드의 최근접 틱에 닫는다'(2패스 오프라인 상한)로 바꾼다 → `PREREG_oracle_best_tick_0911.md` |

## 2026-09-11 추가 (김준태·트랙B)

| # | 미검증 항목 | 비고 |
|---|---|---|
| L73 | **ROS2 위치 한계 위반이 URDF 소수점 절삭에서 온다** | `so101_ver1.urdf` 의 `min_rad`/`max_rad` 가 소수 5자리로 잘려 있다 🟢. wrist_flex 상한 `1.65806` vs 우리 `1.6580627293335335` (차이 2.73e-6 rad), shoulder_pan 하한 `-1.91986` vs `-1.9198621771937616` (2.18e-6 rad). 정책이 계약 정규화 경계 ±1.0 을 명령하면 그만큼 넘어가 **쓰기 주기 전체가 거부된다.** 실측: seed2 에서 wrist_flex 652/18494 프레임, 전부 `max_ratio` 정확히 1.0000x. 클램프하면 성공률 ±0.0%p 로 무해하다 🟢. **조치: (a) ROS·HW 에 URDF 자릿수 정합 요청 (b) 우리 쪽 마이크로 마진 클램프. 둘 다 미착수** |
| L74 | **ROS2 통과율은 하한이다 — controller 보간점을 재현하지 않았다** | `JointTrajectoryController` 는 30Hz 로 보간점을 내고 `SO101System::write` 가 그 보간점을 검사한다 🟢(원문). `probe_exec_envelope` 는 정책 **샘플 명령점**만 검사한다. 위반이 정확히 경계(1.0000x)에서 나는 상황이므로 **보간 overshoot 는 더 걸릴 수 있다.** 재현하려면 `joint_trajectory_controller` 의 보간 규칙(기본 spline)이 필요하다. [ROS] 확인 필요 |
| L75 | **direct-motor 경로는 30Hz 정책 궤적에 부적합하다 — 1.5Hz 를 요구한다** | 시간 스케일링 스윕 실측 🔵(전사식 기준): x1~x10 통과율 0%, **x20(=1.5Hz)에서야 100%**. `waypoint.duration` 100% 위반(dt 0.0333 < 0.05), `peak_accel` 최대 152.6배. L67 의 25.8배 계산보다 실측이 나쁘다 — L67 은 평균 속도, 이건 실제 궤적 최대값이다. **`core/models.py` 원문이 오면 재측정한다.** 속도 검사 `1.875*dq/dt` 는 전사본에 없어 판정에서 제외했다 🟡 |
| L76 | **현행 체크포인트 전부가 실물에 없는 입력을 요구한다 — 그대로는 배포 불가능하다** | 계약·시뮬은 카메라 2대(`cam_front` 3인칭 + `cam_wrist` 손목)를 동시에 먹인다 🟢(`configs/so101.yaml` `count: 2`). 실물은 같은 시점 2대이고 추론 시점에 살아 있는 것은 로봇팔 1대뿐이다 🔵(L62 해소). 따라서 **6/69/25 는 배포 구성 기준 상한**이다. 비용 근사치는 이미 있다 🟢: `donor_front`(front 를 무의미한 입력으로 만든 조건) seed1 69→55, seed2 25→12 — **약 −14%p**. 단 재학습 없는 수치라 **상한**이다 🟡. 조치: `count: 1` 손목 전용 재학습으로 실측 → `PREREG_wrist_only_0911.md`. **해소 (2026-09-12) 🟢: 손목 전용 3시드 55/45/78, 평균 59.3% — 대조군 33.3% 대비 +26.0%p 이고 범위 폭도 63.0→33.0%p 로 줄었다. 합산 95% 구간이 겹치지 않는다([28.2,38.8] vs [53.7,64.7]).** 빼는 편이 성능도 좋으므로 계약을 `count: 1` 로 바꾼다 → D-AI-39. ⚠️ 남는 것: (a) 왜 좋아졌는지 미구분 🟡 (용량 재배분 / 3인칭이 잔차 학습 희석 / 과적합 감소) (b) 이 실행의 EXP_LOG 가 `dirty=True` 라 커밋으로 되짚어갈 수 없다 |
| L77 | **절제 실험 결과를 재학습 결과의 상한으로 쓴 것이 틀렸다** | `donor_front`(front 를 쓸모없는 입력으로 만든 조건, −13~14%p)를 "재학습해도 그보다 나아질 수 없는 상한"이라고 `PREREG_wrist_only_0911.md` 에 적었다 🟡. 실측은 **+26.0%p 상승**이다 🟢. `donor_front` 는 **front 인코더가 살아 있는 채로 쓰레기 입력을 받는** 조건이고 손목 전용은 **front 인코더가 아예 없는** 조건이다 — 같은 축의 강약이 아니라 **다른 모델**이다. 일반화: **입력을 망가뜨리는 절제는 그 입력을 제거한 모델의 성능을 예측하지 않는다.** 앞으로 절제 수치를 구조 변경의 상·하한으로 인용하지 않는다 |
